stream inspector a gara in corso continua a ricaricarsi e vedo la lista continuare ad alternarsi a "no events on this point yet"... lo vorrei stabile.

andrei a distinguere la UI con cui si costruisce il flow dalla UI con cui si gestisce il flow mentre i dati passano.

in particolare la configurazione di source e target (esempio: ip e path di salvataggio) vanno fatti nella costruzione del flow. le action tipo l'insert di dati nel manual flow vanno fatte nella parte di utilizzo a gara in corso. anche lo stream inspector viene usato nella parte di utilizzo. 

oltre allo stream inspector selezionabile per nodo, sarebbe bello poter anche vedere i log per nodo, quelli riconducibili a un nodo. poi a parte mostrerei la possibilità di mostrare tutti i log generali non riconducibili a un nodo (eventualmente con la possibilità di visualizzare anche questi ultimi tutti assieme)

Chiamerei la parte di editor come "Editor" e la parte di gestione "Flow". L'app normalmente si apre su Flow (che è anche home page) e all'inzio c'è solo un messaggio "apri o crea un nuovo flow", se si apre si carica. se si fa nuovo si chiede dove salvarlo e si apre l'editor. nell'editor quando si fa apply si viene mandati alla schermata flow (dove c'è un pulsante modifica che rimanda all'editor - non metterei la tab editor in alto).

l'attuale radio network deve diventare una view/ui custom del modulo TmF.

la connection list la toglierei... mi sembra inutile avendo il grafico che è più comprensibile

mi piace l'idea di filtri riutilizzabili quindi quando se ne crea uno gli darei un nome così:
- diventa ritulizzabile riconoscendolo per nome
- nel grafico del flow sopra il branch mostriamo il nome del filtro che sarà più significativo del "Filter & mapping" che c'è attualmente.

il pulsante export non serve, visto che abbiamo save e save as new

nella ui la scritta "Connected" in alto a destra è equivoca quando si sta visualizzando dentro la finestra del processo (photino) perché l'utente non capisce a cosa si è connessi e soprattutto se la finestra è accesa non si può essere non connessi. mostrerei quell'info solo quando si visualizza da browser.

In un branch fra due nodi oltre che al filtro si deve poter aggiungere un delay per aggiungere latenza nei dati che passano da quel ramo

vorrei usare un framework UI per vue, sono indeciso fra vuetify, daisyui, nuxtui... scegli tu il migliore. vorrei una ui che trasmetta serietà e stabilità.